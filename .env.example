# Copy to .env.local for local development
# Backend base URL for API calls in development (must include trailing slash)
VITE_BACKEND_URL=http://localhost:8080/

# Backend origin for initiating OAuth (used for redirects)
VITE_BACKEND_OAUTH_URL=http://localhost:8080/